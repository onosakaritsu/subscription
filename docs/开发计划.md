# 开发计划

## 当前目标

项目主线为本地优先的订阅管理 Web App。保持 Node.js 原生 HTTP 后端、静态 HTML/CSS/JS 前端、本地 JSON 存储，不使用 Apple Development Team、App Group、WidgetKit、数据库或第三方框架。

## Web V1 任务清单

- [x] 评估无 Apple Development Team 条件下的技术栈。
- [x] 选定零依赖 Node.js 原生 HTTP + 静态前端 SPA + 本地 JSON。
- [x] 取消小组件需求，不再实现 Widget 风格页面或 Widget API。
- [x] 将项目主名改为 `Project_03_subscription-manager-web`。
- [x] 实现订阅领域逻辑：分类、币种、周期、续费日计算、管理排序、统计。
- [x] 实现 HTTP API：增删改查、导入、导出、健康检查。
- [x] 实现前端主界面：列表、表单、搜索、筛选、统计。
- [x] 使用 `data/subscriptions.json` 本地持久化。
- [x] 增加 Node.js 单元测试。
- [x] 清理 SwiftUI / WidgetKit / XcodeGen 遗留文件。
- [x] 同步 README、开发计划、踩坑日志。

## Phase 2：桌面化使用与订阅状态增强

- [x] 保持 V1 基线功能：增删改、自动/手动续费日、筛选、统计、导入导出、健康检查、页面访问。
- [x] 新增续费状态标签：已过期、今日续费、3 日内、7 日内、本月内、正常、已停用。
- [x] 状态计算放入领域层，可复用并有测试覆盖。
- [x] 优化列表默认排序：启用优先、过期最前、停用置底。
- [x] 首页统计卡片：本月需续费、7 日内续费、启用、停用、月度折算、年度折算。
- [x] 多币种分别统计，不做自动汇率换算。
- [x] 新增半年付和一次性周期；一次性不混入周期性折算。
- [x] 增强最近续费摘要，最多显示 5 条紧急续费项目。
- [x] 增加本地自动备份：`data/backups/`，最多保留 20 份。
- [x] 优化 JSON 导出文件名：`subscriptions-backup-YYYY-MM-DD.json`。
- [x] 新增 `start-subscription-manager.sh` 一键启动脚本并设置可执行权限。
- [x] 优化 420px～600px 窄窗口桌面化使用体验。
- [x] 默认 `README.md` 改为中文版，新增 `README.en.md`。
- [x] 补充 Phase 2 测试。

## Phase 3：备份恢复闭环与桌面化体验增强

- [x] 新增备份列表 API：`GET /api/backups`。
- [x] 新增备份预览 API：`GET /api/backups/:fileName`。
- [x] 新增备份恢复 API：`POST /api/backups/:fileName/restore`。
- [x] 恢复前自动创建恢复前备份。
- [x] 导入 JSON 前自动保护当前数据，导入失败不覆盖主数据。
- [x] 前端新增“备份与恢复”区域，支持列表、预览和确认恢复。
- [x] 前端新增轻量续费日历视图：本月续费、下月续费。
- [x] 优化启动脚本，增加 Node.js 检查和端口占用提示。
- [x] 补充 Phase 3 测试。

## Phase 4：功能完善、数据安全闭环与界面收尾

- [x] 新增/编辑订阅弹窗化。
- [x] 新增手动备份、备份下载、外部备份恢复。
- [x] 新增快速启用/停用、确认已续费、复制订阅。
- [x] 增强后端数据校验和高风险写入保护。
- [x] 新增状态筛选、币种筛选、搜索范围扩展和排序选择。
- [x] 完成桌面化界面重构，导入/导出区域下移。
- [x] 建立统一状态色标体系，并适配深色模式可读性。
- [x] 补充 Phase 4 测试。
- [x] 许可证调整为 Apache License 2.0，新增双语 `DISCLAIMER.md`。


## Phase 5：数据完整性、备份演练、币种显示优化与真实浏览器验收

- [x] 完成 Phase 4 PR 合并并推送 `v0.4.0` 标签。
- [x] 币种显示统一为“符号 / ISO 代码 / 中文名称”。
- [x] 新增数据完整性检查 API 和前端卡片。
- [x] 新增备份恢复演练提示。
- [x] 新增未来 12 个月续费事件的 ICS 导出。
- [x] 补充 Phase 5 测试，保持 Phase 4 回归测试通过。
- [x] 允许真实浏览器截图验收，但不提交第三方截图工具依赖。

## v1.0.0 正式可用版收尾

- [x] 合并 Phase 5 并推送 `v0.5.0` 标签。
- [x] 基于最新 main 创建 `release/v1.0.0-ready`。
- [x] 将 `package.json` 版本调整为 `1.0.0`。
- [x] `/api/health` 返回版本号。
- [x] 补齐 favicon，避免浏览器静态资源 404。
- [x] 普通 JSON 导入增加预览和差异摘要。
- [x] 外部备份恢复增加预览和差异摘要。
- [x] 内部备份预览增强币种分布和启用/停用统计。
- [x] 数据完整性检查增强上线提示。
- [x] 启动脚本改为基于脚本所在目录启动。
- [x] 新增 `PRIVACY.md` 和 `RELEASE_CHECKLIST.md`。
- [x] 补充 v1.0.0 测试、API 验证和真实浏览器截图验收。

## 后续建议

- 增加可选自动修复向导，但必须保留预览、确认和恢复路径。
- 增加浏览器通知提醒或系统日历使用指南。
- 增加周期支出趋势图。
- 如数据规模增长，再评估 SQLite 存储适配。
- 如果需要桌面包装，后续单独评估 Tauri 或 Electron。
